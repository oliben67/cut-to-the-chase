.DS_Store
node_modules/
__pycache__/
*.pyc
.venv/
app/demo/data/
stay-the-course/
.prompts/
*.cttc
.coverage
releases/windows/cttc-windows-deploy.zip
releases/windows/cttc-windows-deploy/
.vscode/settings.json
.dev-instructions.json
.catalyst-proj/*
.vscode/*
.claude/worktrees/
# server.py's own runtime data (recording sessions awaiting collection,
# this gateway's persisted .gateway-id) -- instance-specific, never
# meant to be committed (see br-DEDUP-012's gateway id).
app/server/sessions/
# Redis's own RDB/AOF persistence files (see redis_log.py's DEFAULT_DATA_DIR)
# -- instance-specific binary data, never meant to be committed.
app/server/redis-data/