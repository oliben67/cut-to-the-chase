# CTTC Timeline (app)

The Electron + Python application. Full documentation lives in the repository
root: [../README.md](../README.md).

Quick start:

```sh
npm install
npm start            # uv provisions server/.venv (orjson, psutil) on first run
```
