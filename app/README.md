# CTTC Timeline (app)

The Electron client + log-sump-based gateway. Full documentation lives in
the repository root: [../README.md](../README.md).

Quick start:

```sh
npm install
npm start            # starts the local gateway container (Docker required)
```
